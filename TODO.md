- Add `Counter` trait for counters.
- Implement custom reference counting with `Counter` parameter.
- Implement `SyncProfile` to make it possible to conveniently construct presets
  for synchronization primitives.
- Freeze API of stable batteries and `resync::api`.
- Integrate with Nightly rustc's const traits.
